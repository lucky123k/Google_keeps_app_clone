import React from "react";
import ReactDOM from "react-dom";
import "./index.css"
import App from "./App"


ReactDOM.render(<App/>, document.getElementById("root"))


//context api is used for directly contact with parent root to third child ex(parent->child1->child2->child3)